<% unless comment.commenter.nil? %>
    <div class="comment">
        <p>
            <strong><%= comment.commenter %></strong>
        </p>

        <p class="comment-body">
            <%= comment.body %>
        </p>
    </div>
<% end %>
